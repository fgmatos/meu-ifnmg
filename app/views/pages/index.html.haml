-# = render 'shared/image_slide'
        
.container
  .row#stats
    = render "pages/index/stats"
  .row
    .col-md-8.col-sm-12
      -# = render 'shared/tables/slide_tables'
      -# = render 'shared/tables/diarias_servidor'
      -# = render 'shared/tables/diarias_unidade'
  .row
    .col-md-8.col-sm-6
      = render 'shared/slide_motivos'
    .col-md-4.col-sm-6
      = render 'shared/boxmap'
  .row
    -# = render "pages/index/infobox"

:javascript
  $(document).ready(function() { 
    $("#tbtop-servidores").tablesorter(); 
  }); 